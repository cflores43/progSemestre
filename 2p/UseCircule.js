const Circulo = require('../CLASES PARA USE/Circulo')

const Point = require('../CLASES PARA USE/Point2')

let p1 = new Point(2,3)
let R = 5

let HeribertoCirculo = new Circulo(p1,R)

HeribertoCirculo.tostring()